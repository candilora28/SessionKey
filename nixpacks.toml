[phases.setup]
nixPkgs = ['python39', 'pip']

[phases.install]
cmds = ['cd KeyFinder-Server && pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build phase complete"']

[start]
cmd = 'cd KeyFinder-Server && python server.py'
